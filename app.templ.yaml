runtime: python37

env_variables:
  TELEGRAM_API_KEY: "${TELEGRAM_API_KEY}"
  TELEGRAM_WEBHOOK_TOKEN: "${TELEGRAM_WEBHOOK_TOKEN}"
  EXCLUDE_LOG_TIME: "${EXCLUDE_LOG_TIME}"

handlers:
  - url: .*
    secure: always
    script: auto
